#include <stdio.h>
#include <stdlib.h>

/*10.Sa se scrie o functie ce primeste ca argumente un intreg pe 64 biti si un nr intreg fara semn pe 8 biti care va avea
cel mult valoarea maxima reprezentabila pe 4 biti. Functia returneaza de cate ori configuratia binara(pe 4 biti) din numarul dat
ca si al doilea argument se gaseste in numarul dat ca si prim argument(chiar daca se suprapune).
exemplu:
primul nr 1110 1010 0101 0001 (zec: 59985), al doilea nr 1010 (zec 10). Functia va returna 3 deoarece configuratia 1010 se gaseste de 3 ori in
nr initial.*/


int main()
{
    printf("Hello world!\n");
    return 0;
}
